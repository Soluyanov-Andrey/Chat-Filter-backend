# Chat Filter Backend API

## Описание проекта

Это **бэкенд-сервис** на Node.js с использованием Express, реализующий RESTful API для поддержки фронтенд-приложения **Web Components Chat Filter Scanner**. Сервер предоставляет JSON API для операций сканирования, фильтрации и управления HTML-контентом, извлеченным из чатов ChatGPT.

## Архитектурные цели

1. **Изучение и реализация OpenAPI 3.0** - создание полной документации API с использованием спецификации OpenAPI
2. **Комплексное тестирование** - разработка unit и интеграционных тестов для всех эндпоинтов
3. **Сервис-ориентированная архитектура** - создание модульного сервиса, отдающего данные в JSON формате
4. **Интеграция с фронтендом** - обеспечение seamless взаимодействия с веб-компонентами

## Технологический стек

### Основные технологии:
- **Node.js** (v16+)
- **Express.js** - веб-фреймворк
- **OpenAPI 3.0 (Swagger)** - спецификация и документация API
- **Jest/Supertest** - фреймворк для тестирования
- **Dotenv** - управление переменными окружения

### Вспомогательные библиотеки:
- **express-validator** - валидация входных данных
- **cors** - обработка CORS запросов
- **helmet** - безопасность HTTP заголовков
- **morgan** - логирование запросов
- **multer** - обработка загрузки файлов (если требуется)

## Функциональные возможности API

### 1. **Управление файловой структурой**
- Получение структуры папок и файлов
- Навигация по директориям
- Создание новых папок и документов

### 2. **Сканирование HTML контента**
- Анализ сохраненных HTML файлов из чатов
- Извлечение текстового контента
- Парсинг и структурирование информации

### 3. **Фильтрация и сортировка**
- Удаление нерелевантных тем
- Категоризация контента по темам
- Фильтрация по ключевым словам

### 4. **Управление выборками**
- Создание временных страниц из выбранных элементов
- Удаление отфильтрованного контента
- Сохранение отобранных материалов

## Структура API

### Основные эндпоинты:


## 📋 Таблица эндпоинтов API

| Метод | Эндпоинт | Описание | Параметры | Формат ответа | Пример ответа |
|-------|----------|----------|-----------|---------------|---------------|
| **GET** | `/folder-structure` | Возвращает структуру папок в виде JSON | `path` (query) | JSON объект со структурой | `{"status": "completed", "data": {"folders": [...]}}` |
| **GET** | `/open-document` | Открыть document root и прочитать ссылки | `path` (query) | Массив строк с именами файлов | `["строка 1", "строка 2"]` |
| **GET** | `/open-themes` | Открыть document/themes и читает файлы | `path` (query) | Массив строк из файлов | `["строка 1", "строка 2"]` |
| **GET** | `/open-themes-ready` | Открыть готовые темы для просмотра | `path` (query) | Массив с содержанием файла | `["Содержание файла"]` |
| **GET** | `/scan` | Сканировать документ | Нет | Объект с результатами сканирования | `{"status": "completed", "data": [...]}` |
| **GET** | `/view-page` | Посмотреть содержимое страницы | `id` (query) | Объект с результатом | `{"status": "success", "message": "pageview"}` |
| **POST** | `/create-folder` | Создает новую папку по указанному пути | `path` (body) | Объект с результатом | `{"status": "success", "message": "Folder created"}` |
| **POST** | `/create-page` | Создает страницы и темы из документа | `path`, `indexTheme` (body) | Объект с результатом | `{"status": "success", "message": "Folder created"}` |
| **POST** | `/create-topic` | Создает новую тему на страницу | `id`, `path` (body) | Объект с результатом | `{"status": "success", "message": "Folder open"}` |
| **POST** | `/delete-list` | Удаляет тему по ID | `id` (body) | Объект с результатом | `{"status": "success", "message": "Folder open"}` |
| **POST** | `/delete-select` | Удаляет все выделенное галочками | Массив ID (body) | Объект с результатом | `{"status": "success", "message": "File modified"}` |
| **POST** | `/leave-selected` | Оставляет выделенное, остальное удаляет | Массив ID (body) | Объект с результатом | `{"status": "success", "message": "File modified"}` |
| **POST** | `/look-page` | Формирует страницы для просмотра из выделенного | Массив ID (body) | Объект с результатом | `{"status": "success", "message": "File create"}` |



## OpenAPI 3.0 спецификация

Проект включает полную OpenAPI спецификацию (`openapi3.yml`), которая описывает:

### Компоненты спецификации:
1. **Информация о API** - версия, описание, контакты
2. **Серверы** - базовые URL для разных окружений
3. **Пути (Paths)** - все доступные эндпоинты
4. **Компоненты** - переиспользуемые схемы данных
5. **Безопасность** - методы аутентификации (если требуется)

### Пример структуры OpenAPI:
```yaml
openapi: 3.0.0
info:
  title: Chat Filter Scanner API
  version: 1.0.0
  description: API для сканирования и фильтрации HTML контента из ChatGPT

paths:
  /api/structure/{path}:
    get:
      summary: Получить структуру папок
      parameters:
        - name: path
          in: path
          required: true
          schema:
            type: string
      responses:
        '200':
          description: Успешный ответ
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/FolderStructure'
```

## Тестирование

### Типы тестов:
1. **Unit тесты** - тестирование отдельных функций и модулей
2. **Интеграционные тесты** - тестирование взаимодействия с базой данных/файловой системой
3. **API тесты** - тестирование эндпоинтов с помощью Supertest
4. **E2E тесты** - полное тестирование сценариев использования



## Формат данных API

### Пример JSON ответа:
```json
{
  "success": true,
  "message": "Операция выполнена успешно",
  "data": {
    "folders": [
      { "name": "document", "type": "folder+", "size": "2.1MB" },
      { "name": "scripts", "type": "folder-", "size": "450KB" }
    ],
    "files": [
      { "name": "chat_history_2024.html", "type": "file", "size": "1.5MB" }
    ]
  },
  "timestamp": "2024-01-15T10:30:00Z"
}
```


## Интеграция с фронтендом

### Особенности взаимодействия:
1. **CORS настройки** - разрешены запросы с фронтенд домена
2. **Стандартные HTTP коды** - последовательная система ответов
3. **JSON формат** - единый формат обмена данными
4. **Обработка ошибок** - детализированные сообщения об ошибках



## Цели обучения

### Через этот проект изучается:
1. **Проектирование RESTful API** - правильное структурирование эндпоинтов
2. **OpenAPI спецификация** - создание и поддержка документации API
3. **Тестирование Node.js приложений** - полное покрытие кода тестами
4. **Сервисная архитектура** - разделение ответственности между слоями
5. **Интеграция фронтенд-бэкенд** - эффективное взаимодействие между частями системы

## Лицензия и вклад

Проект предназначен для образовательных целей и демонстрации возможностей современного веб-разработки с использованием нативных технологий и лучших практик API дизайна.

---

*Этот бэкенд сервис является неотъемлемой частью полноценного веб-приложения, демонстрирующего, как современные технологии могут работать вместе для решения практических задач обработки информации.*


https://github.com/Soluyanov-Andrey/Chat-Filter-backend/blob/master/document/roma.md