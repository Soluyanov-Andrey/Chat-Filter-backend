
**Условия задачи:** написать документацию в формате Markdown (.md) на основе предоставленных конфигурационных файлов.

**Подход к решению:**

Целесообразно начать с создания общего, краткого описания системы, раскрывающего её назначение и основные принципы работы. На этой основе можно визуализировать возможные архитектурные варианты и логические схемы. В процессе уточнения деталей станет понятно, какие из предложенных вариантов соответствуют реальной системе, а какие — нет. Все последующие изменения и дополнения документации будут вноситься уже на основе обновлённых и уточненных данных о системе.


**Пояснение к документу**

Данный документ является первичным наброском, сборником мыслей и идей. Это **эскиз, а не финальная, готовая к использованию версия документации**.

В процессе изучения материалов я ознакомился с принципами ведения проектной документации и изложил здесь свои выводы в сжатом, обобщённом виде.
Если подходить строго с научной (академической) точки зрения, то это может выглядеть примерно так. Как я опишу ниже.

Однако будем исходить из того, что документация может быть в конечном результате и в более простом виде, не обязательно в строгой академической форме.

Главное — зафиксировать ключевую информацию и структуру. И при необходимости всегда можно развить этот базовый вариант в любом направлении: от простого — к сложному, адаптируя глубину и формальность под конкретные задачи.

**О полноценной корпоративной документации:**

Если говорить о полноценной документации уровня enterprise, она, как правило, структурируется по аудитории и содержит следующие основные разделы:

1.  **«Для начала работы» / Onboarding:** Установка, настройка окружения, обзор технологического стека, запуск первого примера.
2.  **«Для разработчиков» / Developer Guide:** Архитектура системы, описание ключевых процессов, API, принципы расширения и разработки новых модулей.
3.  **«Для пользователей» / User Guide (или «Для бизнеса»):** Инструкции по работе с системой: как редактировать конфигурации, описывать интерфейсы, использовать готовые компоненты.

Дополнительно могут быть описаны **правила и процессы** работы с кодом: flow использования Git (например, GitFlow), правила code review, соглашения о коммитах и ведении проекта.


**Что удалось выяснить из имеющейся информации**
-----------------------

Данная система является мощным декларативным конфигурационным движком уровня **enterprise**, предназначенным для построения пользовательских интерфейсов (экранов, страниц, компонентов). Её ключевая суть заключается в замене традиционного императивного программирования на декларативное описание логики и представления, что позволяет создавать сложные функциональные блоки без написания кода или с его минимальным количеством. Это обеспечивает высокую скорость разработки, единообразие архитектуры и лёгкую поддержку масштабируемых приложений.


⚠️(Эту часть надо уточнять)
### Предполагаемый технологический стек фронтенда
чистый Vue 3 + Vite с кастомным declarative-слоем.

Основу фронтенд-части системы предположительно составляет следующий технологический стек:

| Категория | Технология / Библиотека | Назначение |
| :--- | :--- | :--- |
| **Ядро** | Vue 3 (Composition API) | Основной фреймворк для построения пользовательского интерфейса. |
| **Сборка и Dev-сервер** | Vite | Современный инструмент для сборки и разработки. |
| **Маршрутизация** | Vue Router | Управление навигацией и маршрутами в приложении. |
| **Управление состоянием** | Pinia (или Valtio/MobX для сложных объектов) | Централизованное хранилище состояния приложения. |
| **Работа с API и кэширование** | TanStack Query (Vue Query) | Управление, кэширование и синхронизация серверного состояния. |
| **Стилизация** | Tailwind CSS + Headless UI | Утилитарный CSS-фреймворк и библиотека «невизуальных» компонентов. |
| **Валидация форм** | VeeValidate | Декларативная валидация полей и форм. |
| **Маски ввода** | IMask | Библиотека для применения масок к полям ввода (телефон, дата и т.д.). |
| **Работа с датами** | date-fns | Современная и модульная библиотека для работы с датами и временем. |
| **Коммуникация компонентов** | mitt | Минималистичная шина событий. |
| **Утилиты** | lodash.debounce | Функция для оптимизации частых событий (например, поиск по вводу). |
| ** | @vueuse/integrations | Набор готовых композабел-утилит, включая `useSortable`. |
| **Ядро системы** | **Кастомный декларативный движок** |<br>• **JsonRenderer** – рендеринг интерфейса из конфигурации.<br>• **Action Executor** – выполнение задекларированных действий.<br>• **Layer Merger** – слияние конфигурационных слоёв.<br>• **Config Loader** – загрузка и парсинг конфигураций. |



⚠️(Эту часть надо уточнять)
**Declarative UI Engine**

Исходя из конфигурационного характера движка, который оперирует JSON-деревьями (widgets, vars, sources, actions, on*-хуки) и слоями с возможностью слияния (merge), возникает принципиальный архитектурный вопрос:

**На какой технологической основе реализованы ядро рендеринга и система реактивности этого Declarative UI Engine?**

В частности, для документации необходимо выяснить, использовались ли при разработке (или служили сильным архитектурным вдохновением) следующие готовые библиотеки и механизмы:

*   **Formily (@formily/vue) или FormKit** – для реализации декларативных форм, сложных эффектов, реактивных выражений и логики, управляемой JSON-схемой (schema-based).
*   **Nuxt Layers** (с механизмами `extends`, `glob`/`import.meta.glob` и утилитой `defu` для слияния) – для композиции, наследования и динамической загрузки конфигураций и компонентов.
*   **Другие инструменты декларативного подхода** – такие как JSON Forms, `vue-formily` или кастомный рекурсивный рендерер на основе `<component :is>`.

Ответ на этот вопрос позволит классифицировать движок и понять:

1.  **Уровень кастомизации:** Насколько решение является самописным, а насколько — расширенной готовой платформой.
2.  **Степень связности:** Какие модули движка могут быть легко заменены или дополнены.
3.  **Вектор развития:** Какой путь миграции, эволюции и интеграции с экосистемой является наиболее вероятным и предпочтительным.
📝 Здесь может быть размещена полноценная техническая документация, которая «на пальцах» (то есть максимально наглядно и доступно) описывает процессы преобразования конфигурационных файлов и то, как работает данная система «под капотом».

⚠️(Эту часть надо уточнять)

Из этих размышлений возникает ключевой вопрос: по сути, мы создали декларативный язык (DSL) для описания интерфейсов. Существует ли для него **формальная документация** — спецификация, справочник или гайд?

Без документации и чёткого описания синтаксиса и семантики этого языка разобраться в конфигурационном коде крайне сложно. Это становится серьёзным барьером для нового разработчика или при возвращении к проекту спустя месяцы.

(Разумеется, это идеализированный взгляд. На практике мы часто действуем по ситуации: вспоминаем, пишем, ковыряемся в коде и разбираемся по ходу дела. Реальность я тоже понимаю, но это узкие места в этой системе если расматривать ее в долгой перспективе на подержки)

Отсюда вытекает следующий важный аспект: **эволюция языка.** Меняется ли этот декларативный язык со временем? Когда появляется новая бизнес-задача, можем ли мы реализовать её **исключительно** через конфигурационные файлы, или нам всё равно приходится лезть в сам движок, чтобы добавить или изменить его базовые возможности?





# Генерация маршрутов 
**Генерация маршрутов приложения из единого декларативного конфигурационного файла**

**Версия документа**: 1.0  
**Дата**: февраль 2026  
**Статус**: Внутренняя техническая спецификация

### 1. Обзор механизма

В отличие от классического подхода, где маршруты Vue Router описываются императивно в виде массива объектов `RouteRecordRaw`, в текущей архитектуре **вся структура маршрутов приложения генерируется автоматически из одного декларативного конфигурационного файла**.

Этот файл (обычно `app.routes.storage.ts` или аналогичный) содержит **дерево страниц** в формате:

```ts
export default <Core.StorageConfig>{
  pages: [
    { id: "...", path: "...", meta: { label: "...", alias: "...", grow: true }, children: [...] },
    ...
  ]
}
```

Из этого дерева автоматически строится:

- дерево маршрутов Vue Router
- структура бокового меню
- хлебные крошки
- условная видимость разделов (по ролям)
- мета-данные для каждого маршрута

### 2. Структура конфигурационного файла маршрутов

Ключевые поля на уровне узла:

| Поле              | Тип      | Обязательно | Описание                                                                                     | Пример значения                              |
|-------------------|----------|-------------|----------------------------------------------------------------------------------------------|----------------------------------------------|
| `id`              | string   | да          | Уникальный идентификатор маршрута (используется как name в RouteRecordRaw)                   | `p.apps.create`                              |
| `path`            | string   | да          | Относительный путь сегмента ('' = index)                                                     | `create`, `:appId`, `board`                  |
| `meta.label`      | string   | желательно  | Человеко-читаемое название для меню и хлебных крошек                                         | `Создать заявление`                          |
| `meta.alias`      | string   | опционально | Альтернативное имя маршрута (для ссылок, редиректов, меню)                                   | `p.apps.create`                              |
| `meta.grow`       | boolean  | опционально | Флаг «это группа/раздел» — в меню отображается как заголовок без ссылки                      | `true`                                       |
| `enabled`         | array    | опционально | Условная видимость раздела (выражение if)                                                    | `['if', ['SUBVS_ADMIN', 'in', '*me_role_names.$'], true]` |
| `render`          | array    | опционально | Если `['outlet']` — маршрут является контейнером с `<router-view>`                           | `['outlet']`                                 |
| `children`        | array    | опционально | Вложенные маршруты (рекурсивно)                                                              | [...]                                        |

### 3. Процесс генерации маршрутов

**Функция**: `buildRoutesFromConfig(config: StorageConfig): RouteRecordRaw[]`

**Шаги выполнения**:

1. **Рекурсивный обход дерева**  
   Начинаем с корневого массива `pages`.

2. **Создание RouteRecordRaw для каждого узла**  
   ```ts
   const route: RouteRecordRaw = {
     path: page.path || '',
     name: page.id || page.meta?.alias,
     component: page.render?.[0] === 'outlet'
       ? LayoutOutletComponent
       : defineAsyncComponent(() => import('@/views/DynamicPage.vue')),
     meta: {
       label: page.meta?.label,
       alias: page.meta?.alias,
       grow: page.meta?.grow,
       ...page.meta
     },
     children: page.children ? buildRoutesFromConfig({ pages: page.children }) : undefined,
   };
   ```

3. **Условная видимость**  
   Если есть поле `enabled`, создаётся guard:

   ```ts
   route.beforeEnter = (to, from, next) => {
     if (evaluateCondition(page.enabled, currentUser)) {
       next();
     } else {
       next({ name: 'forbidden' }); // или 403 страница
     }
   };
   ```

4. **Динамические параметры**  
   Параметры вида `:appId`, `:key` автоматически поддерживаются Vue Router.

5. **Регистрация в роутере**  
   После генерации маршруты добавляются в экземпляр роутера:

   ```ts
   generatedRoutes.forEach(route => router.addRoute(route));
   ```

### 4. Связанные механизмы, которые используют ту же карту

| Механизм             | Как использует конфиг роутов                              | Пример вывода (URL /apps/abc123/board)                     |
|----------------------|------------------------------------------------------------|------------------------------------------------------------|
| Боковое меню         | Рекурсивно строит MenuItem[] из label, grow, children     | Заявления → [Создать] → [Просмотр]                         |
| Хлебные крошки       | Идёт вверх по дереву от текущего пути, собирает label     | Главная › Заявления › abc123 › Просмотр                    |
| Активное состояние   | `currentPath.startsWith(fullPath)`                         | подсветка пункта «Заявления»                               |
| Динамические метки   | Замена `:appId` → route.params.appId                       | «abc123» вместо «:appId» в крошках                         |
| Ролевая видимость    | `enabled` → beforeEnter guard + фильтр в меню              | раздел «Настройки» виден только админам                    |

### 5. Преимущества подхода

- **Единая точка истины** — вся навигация, меню, крошки, права в одном файле  
- **Автоматическая согласованность** — изменение конфига сразу обновляет всё  
- **Легко добавлять разделы** — просто дописать узел в дерево  
- **Ролевая безопасность** — встроена на уровне конфига  
- **Поддержка динамических сегментов** — `:id`, `:key` обрабатываются автоматически

### 6. Ограничения и компромиссы

- **Сложность отладки** — при большом дереве трудно найти конкретный маршрут  
- **Производительность** — генерация на старте приложения (решается кэшированием)  
- **Ограниченная гибкость** — для очень сложных guard’ов или middleware может потребоваться кастомный код  
- **Динамические маршруты** — требуют отдельной обработки в breadcrumbs/menu

### 7. Рекомендации по расширению

- Добавить поле `meta.icon` → иконки в меню  
- Поддержка `meta.badge` → счётчик заявлений  
- `meta.order` → явная сортировка пунктов меню  
- `meta.redirect` → автоматические редиректы  
- `meta.hidden` → скрыть из меню, но оставить маршрут

### 8. Пример полной трансформации

**Конфиг**:
```json
pages: [
  { id: "p.dashboard", path: "dashboard", meta: { label: "Рабочий стол" } },
  { id: "p.apps.root", path: "apps", meta: { label: "Заявления", grow: true },
    children: [
      { id: "p.apps.list", path: "", meta: { label: "Список" } },
      { id: "p.apps.create", path: "create", meta: { label: "Создать" } }
    ]
  }
]
```

**Результат**:
- Маршруты: `/dashboard`, `/apps`, `/apps`, `/apps/create`
- Меню:  
  - Рабочий стол  
  - Заявления  
    - Список  
    - Создать  
- Крошки на `/apps/create`: Главная › Заявления › Создать

---





