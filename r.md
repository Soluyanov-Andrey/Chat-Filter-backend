Вот таблица эндпоинтов на основе вашего OpenAPI файла:

## 📋 Таблица эндпоинтов API

| Метод | Эндпоинт | Описание | Параметры | Формат ответа | Пример ответа |
|-------|----------|----------|-----------|---------------|---------------|
| **GET** | `/folder-structure` | Возвращает структуру папок в виде JSON | `path` (query) | JSON объект со структурой | `{"status": "completed", "data": {"folders": [...]}}` |
| **GET** | `/open-document` | Открыть document root и прочитать ссылки | `path` (query) | Массив строк с именами файлов | `["строка 1", "строка 2"]` |
| **GET** | `/open-themes` | Открыть document/themes и читает файлы | `path` (query) | Массив строк из файлов | `["строка 1", "строка 2"]` |
| **GET** | `/open-themes-ready` | Открыть готовые темы для просмотра | `path` (query) | Массив с содержанием файла | `["Содержание файла"]` |
| **GET** | `/scan` | Сканировать документ | Нет | Объект с результатами сканирования | `{"status": "completed", "data": [...]}` |
| **GET** | `/view-page` | Посмотреть содержимое страницы | `id` (query) | Объект с результатом | `{"status": "success", "message": "pageview"}` |
| **POST** | `/create-folder` | Создает новую папку по указанному пути | `path` (body) | Объект с результатом | `{"status": "success", "message": "Folder created"}` |
| **POST** | `/create-page` | Создает страницы и темы из документа | `path`, `indexTheme` (body) | Объект с результатом | `{"status": "success", "message": "Folder created"}` |
| **POST** | `/create-topic` | Создает новую тему на страницу | `id`, `path` (body) | Объект с результатом | `{"status": "success", "message": "Folder open"}` |
| **POST** | `/delete-list` | Удаляет тему по ID | `id` (body) | Объект с результатом | `{"status": "success", "message": "Folder open"}` |
| **POST** | `/delete-select` | Удаляет все выделенное галочками | Массив ID (body) | Объект с результатом | `{"status": "success", "message": "File modified"}` |
| **POST** | `/leave-selected` | Оставляет выделенное, остальное удаляет | Массив ID (body) | Объект с результатом | `{"status": "success", "message": "File modified"}` |
| **POST** | `/look-page` | Формирует страницы для просмотра из выделенного | Массив ID (body) | Объект с результатом | `{"status": "success", "message": "File create"}` |

---

## 📊 Детализированная таблица с параметрами

### GET запросы

| Эндпоинт | Query параметры | Обязательные | Тип | Пример |
|----------|-----------------|--------------|-----|---------|
| `/folder-structure` | `path` | ✅ | string | `?path=/media/andrey/backap/flash` |
| `/open-document` | `path` | ✅ | string | `?path=/media/andrey/backap/flash/document` |
| `/open-themes` | `path` | ✅ | string | `?path=/media/andrey/backap/flash/document/themes` |
| `/open-themes-ready` | `path` | ✅ | string | `?path=/media/andrey/backap/flash/document` |
| `/scan` | — | — | — | — |
| `/view-page` | `id` | ✅ | integer | `?id=1` |

### POST запросы

| Эндпоинт | Body параметры | Обязательные | Тип | Пример |
|----------|----------------|--------------|-----|---------|
| `/create-folder` | `path` | ✅ | string | `{"path": "/новый/путь"}` |
| `/create-page` | `path`, `indexTheme` | ✅ | string, integer | `{"path": "/путь", "indexTheme": 1}` |
| `/create-topic` | `id`, `path` | ✅ | integer, string | `{"id": 123, "path": "/путь"}` |
| `/delete-list` | `id` | ✅ | integer | `{"id": 123}` |
| `/delete-select` | массив ID | ✅ | array[string] | `["1", "2", "3"]` |
| `/leave-selected` | массив ID | ✅ | array[string] | `["1", "2", "3"]` |
| `/look-page` | массив ID | ✅ | array[string] | `["1", "2", "3"]` |

---

## 🔧 Коды ответов

### Успешные ответы:
- **200 OK** - Успешный запрос (GET `/folder-structure`, `/scan`, `/view-page`)
- **201 Created** - Ресурс создан (POST запросы)

### Ошибки:
- **400 Bad Request** - Некорректный запрос
- **403 Forbidden** - Доступ запрещен
- **500 Internal Server Error** - Ошибка на сервере

---

## 📝 Форматы данных

### Структура успешного ответа:
```json
{
  "status": "success",
  "message": "Описание результата",
  "data": {} // или массив
}
```

### Структура ошибки:
```json
{
  "error": "Сообщение об ошибке"
}
```

---

## 🎯 Ключевые моменты API

1. **Все POST запросы** принимают JSON в теле запроса
2. **Все GET запросы с параметрами** используют query-параметры
3. **ID элементов** передаются как числа (integer) или строки в массивах
4. **Пути** всегда передаются как строки
5. **Сканирование** (`/scan`) - единственный GET без параметров
6. **Управление выборками** (`/delete-select`, `/leave-selected`, `/look-page`) работают с массивами ID

---

## 🔄 Сводная таблица операций

| Операция | Метод | Эндпоинт | Назначение |
|----------|-------|----------|------------|
| **Навигация** | GET | `/folder-structure` | Получить структуру папок |
| **Чтение** | GET | `/open-document`, `/open-themes` | Чтение документов и тем |
| **Сканирование** | GET | `/scan` | Сканирование контента |
| **Просмотр** | GET | `/view-page` | Просмотр конкретной страницы |
| **Создание** | POST | `/create-folder`, `/create-page`, `/create-topic` | Создание элементов |
| **Удаление** | POST | `/delete-list`, `/delete-select`, `/leave-selected` | Удаление элементов |
| **Формирование** | POST | `/look-page` | Формирование страниц из выборки |

Эта таблица поможет быстро ориентироваться в API и понимать, какой эндпоинт использовать для конкретной операции.